"use client";
import LoginForm from "@/components/Authentication/Login";
import React from "react";

const page = () => {
  return (
    <div className="grid font-[family-name:var(--font-geist-sans)] mt-8">
      <LoginForm />
    </div>
  );
};

export default page;
